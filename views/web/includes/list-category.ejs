<%
    var categories = (categories) ? categories : [];
%>
<ul class="bsc-list">
    <%
        categories.forEach(c => {
    %>
            <%- include("category", {category: c}); %>
    <%
        })
    %>
</ul>