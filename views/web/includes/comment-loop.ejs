<% 
    var className = (className) ? className : "list-comments";
    var comments = (comments) ? comments : [];
    if (comments.length) {
%>
        <ul class="<%=className%>">
            <% comments.forEach(comment => { %>
                <%- include("comment-item", { comment: comment }) %>
            <% }); %>
        </ul>
<%
    }
%>